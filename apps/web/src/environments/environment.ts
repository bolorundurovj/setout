// Production configuration. The API base URL is relative by default, so the
// single-container deployment (API serves the web app) works with no config.
export const environment = {
  production: true,
  apiBaseUrl: '',
};
