# Setout API

FastAPI backend for Setout. See the repository root README for the full
workflow. Common commands, run from the repository root:

- `make api` runs the API with reload.
- `make migration name=...` creates a migration.
- `make migrate` applies migrations.
- `make test` runs the backend test suite.
