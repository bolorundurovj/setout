# Diagrams

Architecture, dataflow, workflow, and lifecycle diagrams for Setout. The `.html`
files are self-contained; each `.json` is the source spec. Regenerate with the
`archify` skill: `archify deliver <type> <spec>.json <output>.html`.
