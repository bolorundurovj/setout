/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from "@angular/common/http";
import { Observable } from "rxjs";
import { filter, map } from "rxjs/operators";
import { StrictHttpResponse } from "../../strict-http-response";
import { RequestBuilder } from "../../request-builder";

import { PersonRead } from "../../models/person-read";

export interface RestorePerson$Params {
  person_id: string;
}

export function restorePerson(
  http: HttpClient,
  rootUrl: string,
  params: RestorePerson$Params,
  context?: HttpContext,
): Observable<StrictHttpResponse<PersonRead>> {
  const rb = new RequestBuilder(rootUrl, restorePerson.PATH, "post");
  if (params) {
    rb.path("person_id", params.person_id, {});
  }

  return http
    .request(
      rb.build({ responseType: "json", accept: "application/json", context }),
    )
    .pipe(
      filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
      map((r: HttpResponse<any>) => {
        return r as StrictHttpResponse<PersonRead>;
      }),
    );
}

restorePerson.PATH = "/api/people/{person_id}/restore";
