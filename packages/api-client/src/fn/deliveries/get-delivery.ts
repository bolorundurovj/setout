/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from "@angular/common/http";
import { Observable } from "rxjs";
import { filter, map } from "rxjs/operators";
import { StrictHttpResponse } from "../../strict-http-response";
import { RequestBuilder } from "../../request-builder";

import { DeliveryRead } from "../../models/delivery-read";

export interface GetDelivery$Params {
  delivery_id: string;
}

export function getDelivery(
  http: HttpClient,
  rootUrl: string,
  params: GetDelivery$Params,
  context?: HttpContext,
): Observable<StrictHttpResponse<DeliveryRead>> {
  const rb = new RequestBuilder(rootUrl, getDelivery.PATH, "get");
  if (params) {
    rb.path("delivery_id", params.delivery_id, {});
  }

  return http
    .request(
      rb.build({ responseType: "json", accept: "application/json", context }),
    )
    .pipe(
      filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
      map((r: HttpResponse<any>) => {
        return r as StrictHttpResponse<DeliveryRead>;
      }),
    );
}

getDelivery.PATH = "/api/deliveries/{delivery_id}";
