/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from "@angular/common/http";
import { Observable } from "rxjs";
import { filter, map } from "rxjs/operators";
import { StrictHttpResponse } from "../../strict-http-response";
import { RequestBuilder } from "../../request-builder";

import { VendorAgreements } from "../../models/vendor-agreements";

export interface GetVendorAgreements$Params {
  vendor_id: string;
}

export function getVendorAgreements(
  http: HttpClient,
  rootUrl: string,
  params: GetVendorAgreements$Params,
  context?: HttpContext,
): Observable<StrictHttpResponse<VendorAgreements>> {
  const rb = new RequestBuilder(rootUrl, getVendorAgreements.PATH, "get");
  if (params) {
    rb.path("vendor_id", params.vendor_id, {});
  }

  return http
    .request(
      rb.build({ responseType: "json", accept: "application/json", context }),
    )
    .pipe(
      filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
      map((r: HttpResponse<any>) => {
        return r as StrictHttpResponse<VendorAgreements>;
      }),
    );
}

getVendorAgreements.PATH = "/api/vendors/{vendor_id}/agreements";
